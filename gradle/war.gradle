apply plugin: "war"

bootWar {
    mainClassName = "com.cuppafame.govgains.GovgainsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
